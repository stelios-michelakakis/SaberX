services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: saberx
      POSTGRES_USER: saberx
      POSTGRES_PASSWORD: saberx
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U saberx -d saberx"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build: .
    environment:
      DATABASE_URL: postgres://saberx:saberx@postgres:5432/saberx
      APP_ORIGIN: https://localhost
      SESSION_COOKIE_NAME: saberx_session
      INVITATION_SECRET: change-me-before-use
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3000:3000"

  worker:
    build: .
    command: ["npm", "run", "worker"]
    environment:
      DATABASE_URL: postgres://saberx:saberx@postgres:5432/saberx
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
